apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: flask-reader-binding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: flask-app
    namespace: default
roleRef:
  kind: Role
  name: flask-reader
  apiGroup: rbac.authorization.k8s.io
